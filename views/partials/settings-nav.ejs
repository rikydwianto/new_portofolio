<div class="settings-nav">
    <a href="/admin/settings/profile"
        class="nav-item <%= typeof activeTab !== 'undefined' && activeTab === 'profile' ? 'active' : '' %>">👤 Profile &
        Hero</a>
    <a href="/admin/settings/stats"
        class="nav-item <%= typeof activeTab !== 'undefined' && activeTab === 'stats' ? 'active' : '' %>">📊 Stats</a>
    <a href="/admin/settings/badges"
        class="nav-item <%= typeof activeTab !== 'undefined' && activeTab === 'badges' ? 'active' : '' %>">💫 Orbit
        Badges</a>
    <a href="/admin/settings/skills"
        class="nav-item <%= typeof activeTab !== 'undefined' && activeTab === 'skills' ? 'active' : '' %>">🛠️ Skill
        Cards</a>
</div>
<style>
    .settings-nav {
        display: flex;
        gap: 0.5rem;
        margin-bottom: 2rem;
        border-bottom: 1px solid #E2E8F0;
        padding-bottom: 1rem;
        overflow-x: auto;
    }

    .nav-item {
        padding: 0.5rem 1rem;
        border-radius: 6px;
        font-weight: 600;
        font-size: 0.9rem;
        color: #64748B;
        white-space: nowrap;
    }

    .nav-item:hover {
        background: #F1F5F9;
        color: #334155;
    }

    .nav-item.active {
        background: #E0E7FF;
        color: #4F46E5;
    }
</style>